package com.example.employeeservice.messaging;

import org.springframework.amqp.core.Queue;
import org.springframework.amqp.rabbit.core.RabbitTemplate;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.stereotype.Component;

@Component
@Configuration
public class EmployeeMessageProducer {
    public static final String EMPLOYEE_QUEUE = "employee.queue";

    @Bean
    public Queue employeeQueue() {
        return new Queue(EMPLOYEE_QUEUE, true);
    }

    @Autowired
    private RabbitTemplate rabbitTemplate;

    public void sendEmployeeUpdate(Long employeeId) {
        rabbitTemplate.convertAndSend(EMPLOYEE_QUEUE, "Employee " + employeeId + " updated");
    }
}